<ul id='sortable_list'>
  <%= render :partial => 'part', :collection => @carousel_parts %>
</ul>
<%= render :partial => "/refinery/admin/sortable_list",
            :locals => {
              :continue_reordering => (local_assigns.keys.include?(:continue_reordering)) ? continue_reordering : true
            } %>
